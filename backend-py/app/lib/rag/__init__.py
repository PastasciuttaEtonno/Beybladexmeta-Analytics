"""Retrieval-augmented generation over the Beyblade X knowledge base.

The qualitative corpus lives here. The statistics do not: numbers are answered
from unified_meta_view and the *_stats tables by parameterised queries, never by
similarity search, because a vector index cannot order, sum or compare.
"""
