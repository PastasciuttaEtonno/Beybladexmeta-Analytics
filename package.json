{
  "name": "beybladexmeta-analytics",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Monorepo root. frontend/ (Vite + React), backend/ (Express, being strangled) and backend-py/ (FastAPI, taking over route by route).",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "check": "npm --prefix frontend run check",
    "db:down": "docker compose -f docker-compose.dev.yml down",
    "db:migrate": "python tools/migrate.py",
    "db:psql": "docker exec -it beyblade-dev-db psql -U postgres -d beyblade_tracker",
    "db:reset": "docker compose -f docker-compose.dev.yml down -v && docker compose -f docker-compose.dev.yml up -d",
    "db:up": "docker compose -f docker-compose.dev.yml up -d",
    "dev": "concurrently -n fastapi,web -c yellow,magenta \"npm run dev:py\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:legacy": "concurrently -n express,fastapi,web -c cyan,yellow,magenta \"npm --prefix backend run dev\" \"npm run dev:py\" \"npm --prefix frontend run dev\"",
    "dev:py": "uv run --directory backend-py uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload",
    "install:all": "npm --prefix frontend install && uv sync --directory backend-py",
    "legacy:build": "npm --prefix backend run build",
    "legacy:check": "npm --prefix backend run check",
    "legacy:install": "npm --prefix backend install",
    "strangler:parity": "python tools/parity.py",
    "strangler:parity-admin": "python tools/parity_admin_writes.py",
    "strangler:parity-auth": "uv run --directory backend-py python ../tools/parity_auth.py",
    "strangler:parity-claim": "python tools/parity_challonge_claim.py",
    "strangler:parity-oauth": "python tools/parity_oauth.py",
    "strangler:parity-scoring": "python tools/parity_scoring.py",
    "strangler:parity-tournaments": "python tools/parity_tournament_writes.py",
    "strangler:parity-writes": "python tools/parity_writes.py",
    "strangler:session": "python tools/dev_session.py"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
