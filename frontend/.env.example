# Frontend service configuration. Copy to .env and fill in. Only VITE_* reach the browser.

# Dev server proxy target for /api and /sitemap.xml
BACKEND_URL=

VITE_PUBLIC_MINIO_URL=
VITE_RECAPTCHA_SITE_KEY=
VITE_RECAPTCHA_USE_ENTERPRISE=
